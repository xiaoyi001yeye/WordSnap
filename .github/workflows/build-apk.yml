name: Build WordSnap APK

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  pull_request:
    paths:
      - "lib/**"
      - "pubspec.yaml"
      - ".github/workflows/build-apk.yml"

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: zulu
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.27.0"
          channel: stable
          cache: true

      - name: Generate Android scaffold
        run: |
          if [ ! -d android ]; then
            flutter create --platforms=android --project-name wordsnap .
          fi

      - name: Get dependencies
        run: flutter pub get

      - name: Build release APKs
        run: flutter build apk --release --split-per-abi

      - name: Rename APK artifacts
        run: |
          cp build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk \
            build/app/outputs/flutter-apk/wordsnap-armeabi-v7a-release.apk
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
            build/app/outputs/flutter-apk/wordsnap-arm64-v8a-release.apk
          cp build/app/outputs/flutter-apk/app-x86_64-release.apk \
            build/app/outputs/flutter-apk/wordsnap-x86_64-release.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: wordsnap-release-apks
          path: |
            build/app/outputs/flutter-apk/wordsnap-armeabi-v7a-release.apk
            build/app/outputs/flutter-apk/wordsnap-arm64-v8a-release.apk
            build/app/outputs/flutter-apk/wordsnap-x86_64-release.apk
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build-apk
    runs-on: ubuntu-latest

    steps:
      - name: Download APK artifact
        uses: actions/download-artifact@v4
        with:
          name: wordsnap-release-apks
          path: dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/wordsnap-armeabi-v7a-release.apk
            dist/wordsnap-arm64-v8a-release.apk
            dist/wordsnap-x86_64-release.apk
          generate_release_notes: true
